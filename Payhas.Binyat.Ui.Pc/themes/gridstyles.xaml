<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                    xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing"
                    xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
                    xmlns:cnv="clr-namespace:Payhas.Binyat.Ui.Pc.Converters">
  <Style x:Key="QuantityDisplayFormat" TargetType="{x:Type dxg:GridColumn}">
    <Setter Property="dxg:ColumnBase.EditSettings">
      <Setter.Value>
        <dxe:SpinEditSettings DisplayFormat="###,###,###,###,##0.##" AllowDefaultButton="False"/>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="AmountDisplayFormat" TargetType="{x:Type dxg:GridColumn}">
    <Setter Property="dxg:ColumnBase.EditSettings">
      <Setter.Value>
        <dxe:SpinEditSettings DisplayFormat="###,###,###,###,##0.00" AllowDefaultButton="False"/>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="LinesCountSummaryStyle" TargetType="{x:Type ContentPresenter}">
    <Setter Property="ContentPresenter.ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <StackPanel HorizontalAlignment="Left">
            <TextBlock HorizontalAlignment="Left" Foreground="{StaticResource grayForegroundBrush}"
                       TextTrimming="CharacterEllipsis" TextAlignment="Left" IsHitTestVisible="false">
              <Run Text="{Localize 'Lines Count'}"/>
              <LineBreak/>
              <Run Foreground="{StaticResource blackForegroundBrush}" FontWeight="Bold"
                   Text="{Binding Column.TotalSummaryText, Mode=OneWay}"/>
            </TextBlock>
          </StackPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="QuantitiesSumSummaryStyle" TargetType="{x:Type ContentPresenter}">
    <Setter Property="ContentPresenter.ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <StackPanel HorizontalAlignment="Left">
            <TextBlock HorizontalAlignment="Left" Foreground="{StaticResource grayForegroundBrush}"
                       TextTrimming="CharacterEllipsis" TextAlignment="Left" IsHitTestVisible="false">
              <Run Text="{Localize 'Quantities Sum'}"/>
              <LineBreak/>
              <Run Foreground="{StaticResource blackForegroundBrush}" FontWeight="Bold"
                   Text="{Binding Column.TotalSummaryText, Mode=OneWay}"/>
            </TextBlock>
          </StackPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="TotalSummaryStyle" TargetType="{x:Type ContentPresenter}">
    <Setter Property="ContentPresenter.ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <StackPanel HorizontalAlignment="Right">
            <TextBlock HorizontalAlignment="Right" Foreground="{StaticResource grayForegroundBrush}"
                       TextTrimming="CharacterEllipsis" TextAlignment="Right" IsHitTestVisible="false">
              <Run Text="{Localize Total}"/>
              <LineBreak/>
              <Run Foreground="{StaticResource blackForegroundBrush}" FontWeight="Bold"
                   Text="{Binding Column.TotalSummaryText, Mode=OneWay}"/>
            </TextBlock>
          </StackPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="SentTotalSummaryStyle" TargetType="{x:Type ContentPresenter}">
    <Setter Property="ContentPresenter.ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <StackPanel HorizontalAlignment="Right">
            <TextBlock HorizontalAlignment="Right" Foreground="{StaticResource grayForegroundBrush}"
                       TextTrimming="CharacterEllipsis" TextAlignment="Right" IsHitTestVisible="false">
              <Run Text="{Localize 'Sent Total'}"/>
              <LineBreak/>
              <Run Foreground="{StaticResource blackForegroundBrush}" FontWeight="Bold"
                   Text="{Binding Column.TotalSummaryText, Mode=OneWay}"/>
            </TextBlock>
          </StackPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ReceivedTotalSummaryStyle" TargetType="{x:Type ContentPresenter}">
    <Setter Property="ContentPresenter.ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <StackPanel HorizontalAlignment="Right">
            <TextBlock HorizontalAlignment="Right" Foreground="{StaticResource grayForegroundBrush}"
                       TextTrimming="CharacterEllipsis" TextAlignment="Right" IsHitTestVisible="false">
              <Run Text="{Localize 'Received Total'}"/>
              <LineBreak/>
              <Run Foreground="{StaticResource blackForegroundBrush}" FontWeight="Bold"
                   Text="{Binding Column.TotalSummaryText, Mode=OneWay}"/>
            </TextBlock>
          </StackPanel>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="GridRowNo" TargetType="{x:Type dxg:GridColumn}">
    <Setter Property="dxg:BaseColumn.Header" Value="#"/>
    <Setter Property="dxg:BaseColumn.Width" Value="30"/>
    <Setter Property="dxg:ColumnBase.AllowEditing" Value="False"/>
    <Setter Property="dxg:ColumnBase.CellTemplate">
      <Setter.Value>
        <DataTemplate>
          <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                     Text="{Binding RowData.RowHandle.Value, Converter={cnv:ValueIncreaserConverter}}"/>
        </DataTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="dxg:ColumnBase.PrintCellStyle">
      <Setter.Value>
        <Style TargetType="{x:Type dxe:TextEdit}"
               BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
          <Setter Property="dxp:TextExportSettings.Text" Value="{Binding RowData.RowHandle.Value}"/>
          <Setter Property="dxp:ExportSettings.TargetType" Value="Text"/>
        </Style>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
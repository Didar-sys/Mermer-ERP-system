<mvx:MvxWpfView x:Class="Payhas.Binyat.Ui.Pc.Views.LoginView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                xmlns:mvx="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
                xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                xmlns:vm="clr-namespace:Payhas.Binyat.Ui.Core.ViewModels;assembly=Payhas.Binyat.Ui.Core">
  <DockPanel>
    <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right">
      <Button Content="{Localize 'Open Settings'}" Command="{Binding ShowSettingsCommand}"/>
      <Button Content="{Localize 'Activate Application'}" Command="{Binding ShowActivationCommand}"/>
    </StackPanel>
    <Grid>
      <dxlc:LayoutControl VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical"
                          IsEnabled="{Binding IsBusy, Converter={dxmvvm:BooleanNegationConverter}}">
        <Image Source="../Resources/binyat-icon-004.png" Width="240" Height="240"/>
        <dxlc:LayoutGroup Orientation="Horizontal" Margin="0,0,0,50">
          <TextBlock TextAlignment="Right" Text="{Localize AppName}"/>
          <TextBlock x:Name="VersionText"/>
        </dxlc:LayoutGroup>
        <dxlc:LayoutGroup Orientation="Vertical" Width="300">
          <dxlc:LayoutItem Label="{Localize Username}">
            <dxe:TextEdit EditValue="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                          FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"/>
          </dxlc:LayoutItem>
          <dxlc:LayoutItem Label="{Localize Password}">
            <dxe:PasswordBoxEdit EditValue="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
          </dxlc:LayoutItem>
          <dxlc:LayoutItem HorizontalAlignment="Right">
            <Button IsDefault="true" Command="{Binding LoginCommand}" Content="{Localize Login}"/>
          </dxlc:LayoutItem>
        </dxlc:LayoutGroup>
      </dxlc:LayoutControl>
      <Grid dx:ThemeManager.ThemeName="HybridApp"
            Visibility="{Binding IsBusy, Converter={dxmvvm:BooleanToVisibilityConverter}}">
        <dx:WaitIndicator DeferedVisibility="{Binding IsBusy}"/>
      </Grid>
    </Grid>
  </DockPanel>
</mvx:MvxWpfView>
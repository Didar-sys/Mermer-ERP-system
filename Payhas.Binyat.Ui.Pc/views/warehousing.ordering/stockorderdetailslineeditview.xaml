<mvx:MvxWpfView x:Class="Payhas.Binyat.Ui.Pc.Views.Warehousing.Ordering.StockOrderDetailsLineEditView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                xmlns:mvx="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
                xmlns:vm="clr-namespace:Payhas.Binyat.Ui.Core.ViewModels.Warehousing.Ordering;assembly=Payhas.Binyat.Ui.Core">
  <DockPanel>
    <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right" Orientation="Horizontal"
                dx:ThemeManager.ThemeName="Office2013DarkGray;Touch">
      <Button IsDefault="true" Content="{Localize Update}" Command="{Binding SaveCommand}"/>
      <Button IsCancel="true" Margin="10,0,0,0" Content="{Localize Cancel}" Command="{Binding CloseCommand}"/>
    </StackPanel>
    <Grid FocusManager.FocusedElement="{Binding ElementName=FirstFocus}">
      <dxlc:LayoutControl Width="400" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center"
                          IsEnabled="{Binding IsBusy, Converter={dxmvvm:BooleanNegationConverter}}">
        <dxlc:LayoutItem Label="{Localize 'Stock Code'}">
          <dxe:TextEdit IsReadOnly="true" EditValue="{Binding Details.StockCode, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="{Localize 'Stock Name'}">
          <dxe:TextEdit IsReadOnly="true" EditValue="{Binding Details.StockName, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="{Localize Quantity}">
          <dxlc:LayoutGroup>
            <dxe:SpinEdit x:Name="FirstFocus" MinValue="0"
                          EditValue="{Binding Details.Quantity, UpdateSourceTrigger=PropertyChanged}"/>
            <dxg:LookUpEdit AutoPopulateColumns="false" DisplayMember="Name" ValueMember="Id"
                            EditValue="{Binding Details.UnitId, UpdateSourceTrigger=PropertyChanged}"
                            ItemsSource="{Binding Details.Units}">
              <dxe:PopupBaseEdit.PopupContentTemplate>
                <ControlTemplate>
                  <dxg:GridControl Name="PART_GridControl" AutoExpandAllGroups="true">
                    <dxg:GridControl.Columns>
                      <dxg:GridColumn FieldName="Name"/>
                    </dxg:GridControl.Columns>
                    <dxg:GridControl.View>
                      <dxg:TableView AutoWidth="true"/>
                    </dxg:GridControl.View>
                  </dxg:GridControl>
                </ControlTemplate>
              </dxe:PopupBaseEdit.PopupContentTemplate>
            </dxg:LookUpEdit>
          </dxlc:LayoutGroup>
        </dxlc:LayoutItem>
      </dxlc:LayoutControl>
      <Grid Visibility="{Binding IsBusy, Converter={dxmvvm:BooleanToVisibilityConverter}}">
        <dx:WaitIndicator DeferedVisibility="{Binding IsBusy}"/>
      </Grid>
    </Grid>
  </DockPanel>
</mvx:MvxWpfView>